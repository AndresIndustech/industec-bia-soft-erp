{
    "name": "industec/ot-lib",
    "description": "Librerías de la app de OTs que no van en la carpeta web: dompdf, para el PDF de la orden (T2.13, la 008). Ver LEEME.md.",
    "type": "project",
    "require": {
        "php": ">=8.1",
        "dompdf/dompdf": "^3.1"
    },
    "config": {
        "optimize-autoloader": true,
        "sort-packages": true
    }
}
