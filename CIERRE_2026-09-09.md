# Cierre de jornada — 2026-09-09

> Para retomar: lee **§1 (lo que quedó vivo)** y **§4 (lo que sigue)**. El resto
> es el detalle de por qué está como está.

---

## 1. Lo que quedó vivo y corriendo

| Pieza | Dónde | Estado |
|---|---|---|
| **MariaDB** | estación | Servicio de Windows, arranque automático |
| **Vigilante del buzón** | estación, Tarea programada | IMAP IDLE, empuja a Hostinger. Perro guardián cada 10 min |
| **Informes de OT** | estación, Tarea programada | Cada 3 h. Actualiza quién atendió qué |
| **Sitio de pruebas** | `darkviolet-armadillo-872352.hostingersite.com/ot/` | 22 usuarios, buzón operativo |
| **Despliegue** | `t2_10_desplegar.py` | Por SSH, verificado por hash. Se acabó el administrador de archivos |

**Enlace del sistema:** https://darkviolet-armadillo-872352.hostingersite.com/ot/login.php

### Cómo comprobar que sigue todo en pie

```powershell
Get-Service MariaDB | Select-Object Status, StartType          # Running / Automatic
Get-ScheduledTask | Where-Object { $_.TaskName -like "INDUSTEC*" } | Select-Object TaskName, State
```

```bash
cd "D:\INDUSTECH IA\desarrollo\agentes"
Get-Content (Get-ChildItem logs\vigilante-*.log | Sort-Object LastWriteTime | Select-Object -Last 1) -Tail 5
```

El vigilante en reposo **no escribe nada**: solo registra al arrancar, al
detectar un correo y al empujar. Silencio es lo normal.

---

## 2. Los números al cierre

| | |
|---|---|
| Casos del buzón (ventana de 90 días) | **914** |
| Ya atendidos, con informe | **107** |
| Con orden de cierre, esperando confirmación en SAP | **30** |
| Cerrados por falta de atención, pendientes de regularizar | **735** |
| Usuarios creados | **22** (2 superadmin, 1 administración, 3 jefes, 16 técnicos) |
| Informes de OT en el servidor | **116** PDFs, 54 MB, fuera del alcance web |

---

## 3. Lo que se construyó, en orden

1. **Padrón de técnicos** — 40 personas, distinguiendo «sigue trabajando» de
   «salió sin fecha registrada». Corrigió un hallazgo del proyecto: 1.362
   órdenes figuraban «fuera de nómina» y su autor trabaja hoy.
2. **Los 19 usuarios** — el nombre de usuario se calcula del padrón y se compara
   contra la lista aprobada; si el Excel cambia un apellido, aborta.
3. **Buzón de casos** — alcance por zona en el servidor, alertas que no deciden.
4. **Sincronización en vivo** — IMAP IDLE, empuje firmado con HMAC y caducidad.
5. **Qué se atendió y por quién** — cruce de los informes de OT del buzón.
6. **Buzón operativo** — asignar, derivar, revisión, veredicto, cierre en dos
   manos, regularizar. Todo persiste y todo queda registrado.
7. **Órdenes emitidas y visor de PDF** — con enlace compartible que caduca.
8. **Bitácora minable** — cada fila es una transición, con datos en JSON.

---

## 4. Lo que sigue

### Decidido y pendiente de hacer

| # | Qué | Por qué importa |
|---|---|---|
| 1 | **Cerrar `catalogos.php`** con sesión | Hoy responde 200 a cualquiera con 100 locales, 19 técnicos y 914 casos con 206 usuarios de KFC identificables. Es lo más urgente que queda |
| 2 | **Emitir la orden desde el sistema** | Hoy la emite el sistema viejo y por eso hay que leer los informes del correo. Al mover esto, `t2_11` se elimina |
| 3 | **Cronograma de preventivos operativo** | La pantalla existe y muestra; falta que agendar y reagendar escriban |
| 4 | **Tableros** | Casos por antigüedad, cumplimiento, carga por técnico |
| 5 | **Minería de la bitácora** | Las seis consultas están escritas en `sql/005`. Falta correrlas cada tanto y avisar |

### Abierto, necesita decisión

| Qué | Situación |
|---|---|
| **El 36% del correctivo que no llega al buzón** | Sin causa establecida. Haría falta leer los tres buzones de zona |
| **El conteo de casos se movía entre corridas** | El lector es determinista (dos corridas idénticas); el buzón cambiaba. **No se estableció el mecanismo exacto** de las bajas. El `antes=` de `sync.log` deja ver cualquier caída |
| **Contrato de encargo con INDUSTEC** | Pendiente. Ver `DECISION_ARQUITECTURA_Y_DATOS.md` |
| **Delegado de protección de datos de INDUSTECH** | Plazo vencido. Encargado a otra IA con `BRIEF_DPD_INDUSTECH.md` |
| **Cuenta de demostración para técnico** | Hoy hay que usar la clave impresa de una persona real y cambiarla. Si se quiere probar sin eso, hay que crear una cuenta aparte |

---

## 5. Lo que hay que saber para no romper nada

**Las claves de los 19 técnicos** están en `SALIDAS IA\OTS\CLAVES_TECNICOS_imprimir.html`,
fuera de git. La base solo guarda el hash: si se pierden, hay que generar nuevas
una por una desde Usuarios.

**En Hostinger solo se toca el sitio de pruebas.** Los otros tres del panel
—incluido el sistema en producción con el que INDUSTEC factura hoy— no se
modifican. Y ningún trámite que genere un cobro. Es la regla 9 de `CLAUDE.md`.

**`nucleo/config.php` del sitio de pruebas no se sobreescribe**: lleva las
credenciales de la base y el secreto de sincronización. Está en la lista de
prohibidos de `t2_10_desplegar.py`.

**La ruta de destino del despliegue es una compuerta que aborta.** Apuntarla a
otro sitio corta antes de conectar. Verificado.

**El vigilante y los informes leen el correo en SOLO LECTURA** (EXAMINE +
BODY.PEEK). No marcan, no mueven, no borran. Es una restricción del cliente.

---

## 5b. Hay otra conversación trabajando en paralelo

Al cerrar esta jornada, la conversación **«interfaz moderna»** estaba a medio
camino en `desarrollo/sistema_ots/app/publico/`: `nucleo/Ui.php`, `ui.js`,
`graficos.js`, `estilo.css`, cambios en `Casos.php` y `Reconciliar.php`, y una
migración `sql/007_repuestos_y_captura.sql`. Está anotada en §5.1 de
`ESTADO.md`, como manda la regla.

**Nada de eso se confirmó en git desde aquí ni se desplegó**, y con razón: el
`Casos.php` local ya depende de `Ui.php`, que todavía no está en el servidor.
Desplegarlo habría tumbado todas las pantallas. Antes de subir cualquier cosa
de `app/publico/`, comprueba que esa conversación haya terminado.

Lo que sí quedó confirmado desde aquí son los cuatro scripts de la estación y
este documento.

---

## 6. Errores que se cometieron y cómo se detectaron

Se listan porque el patrón se repite: **casi todos aparecieron al probar de
verdad, no al escribir el código.**

| Qué falló | Cómo salió |
|---|---|
| `.gitignore` protegía una ruta que no existe; la clave de la base iba a entrar al repositorio | Al verificar contra el archivo real en vez de contra el patrón |
| `usuarios.tecnico_id` iba a apuntar a otra persona tras cada recarga | Al preguntarse qué pasa con los ids en un DELETE + INSERT |
| Un técnico veía los 300 casos de su zona con el usuario de KFC de cada uno | Al probar con un usuario técnico |
| «911 de 918 pasados de fecha» como titular: cierto y engañoso | Al mirar la captura y no creerse el número |
| `zip()` emparejando mensajes con respuestas: perdía correos en silencio | Al notar una diferencia de 1 entre dos corridas |
| El vigilante llevaba dos horas muerto después de decir que estaba corriendo | Al comprobar el proceso en vez de fiarse del arranque |
| `Auth::exigir()` guardaba sus denegaciones como exitosas | Al correr las propias consultas de detección |
| El vigilante reconectaba cada 60 s en vez de cada 24 min | Al leer el log con atención |

---

*Cerrado el 2026-09-09. Todo confirmado en git; el historial de la jornada son
los siete commits desde `0938243`.*
